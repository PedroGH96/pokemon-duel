package com.pokemonduel.model.enums;

/** Raridade da figura. Determina drop rate nas caixas de recompensa. */
public enum Rarity {
    C,   // Comum       — drop 60%
    UC,  // Incomum     — drop 25%
    R,   // Raro        — drop 10%
    EX,  // EX          — drop 4%
    UX   // Ultra EX    — drop 1%
}
